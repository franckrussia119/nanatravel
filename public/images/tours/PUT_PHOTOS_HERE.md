# Tour photos go here

One photo per tour. File names must match the `image` field in
`data/tours.ts`, e.g. `kribi-1.jpg`, `mount-cameroon-1.jpg`, etc.

Until a file exists, that tour shows a colored placeholder instead.
